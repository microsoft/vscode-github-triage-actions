{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"type": "object",
	"properties": {
		"$schema": {
			"type": "string"
		}
	},
	"additionalProperties": {
		"type": "object",
		"properties": {
			"skipLabeling": {
				"description": "If true, this label will not be autoimatically applied to issues",
				"type": "boolean"
			},
			"comment": {
				"description": "Comment to add to issues detected to be in this feature area",
				"type": "string"
			},
			"assign": {
				"description": "List of usernames to assign to issues detected to be in this feature area",
				"type": "array",
				"items": {
					"type": "string"
				}
			}
		}
	}
}